# Core (Gencat ingest, EFFIS, grid, publish) — stdlib + light deps
requests>=2.31.0
pandas>=2.0.0
pyarrow>=14.0.0

# Geometry / rasterize onto app grid
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0

# CDSE openEO (map_scars_openeo.py) + local GeoTIFF polygonize
openeo>=0.30.0
rasterio>=1.3.0
